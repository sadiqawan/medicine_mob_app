/*
import 'package:get/get.dart';

class UserChatController extends GetxController {
  var messages = <Map<String, String>>[].obs;

  @override
  void onInit() {
    super.onInit();
    // Add a default message from the other user when the screen loads
    messages.add({"sender": "other", "message": "Hello! How can I help you today?"});
  }

  void sendMessage(String message) {
    if (message.trim().isNotEmpty) {
      messages.add({"sender": "user", "message": message.trim()});
    }
  }
}
*/
